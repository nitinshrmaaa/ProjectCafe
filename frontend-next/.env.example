# Base URL of the Brew Haven Express API (see ../backend).
# When this server is unreachable the frontend falls back to local handling
# for reservations, contact messages and auth so the UI stays usable.
NEXT_PUBLIC_API_URL=http://localhost:5000/api
